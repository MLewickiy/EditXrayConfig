# Минимальная версия Python
# Рекомендуется Python 3.11+
# GUI фреймворк
PyQt6>=6.6.0

# Утилиты для работы с URL и JSON (стандартные библиотеки Python, для информации)
# json, urllib.parse, pathlib — встроенные, указывать не нужно, просто для понимания

# Дополнительно (если понадобятся функции проверки ссылок, валидации UUID)
validators>=0.24.0
